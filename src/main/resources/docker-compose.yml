version: '3'
services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - USER_SERVICE_URL=${user.service.url}
      - USER_SERVICE_USERNAME=${user.service.username}
      - USER_SERVICE_PASSWORD=${user.service.password}